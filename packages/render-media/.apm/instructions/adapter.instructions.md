---
description: "📦 アダプタの契約（サービス固有の実装が満たすもの）"
applyTo: "**/tools/render/**,**/media/**"
---

# 📦 アダプタの契約（サービス固有の実装が満たすもの）

> この型はサービスを知らない。知っているのは**アダプタが満たすべき契約**だけである。
> なぜ分けるかは [overview.md](overview.instructions.md)。
> 実行手続きは skill [render-media](../skills/render-media/SKILL.md)。

## アダプタはホストが持つ

**共有ハーネスにサービス固有の実装を置かない。** ElevenLabs でも Stable Diffusion でも、
それは「そのプロジェクトの都合」であって、動画を作らないプロジェクトに依存を持ち込む理由はない。

ハーネスは契約を定め、**ホストが `tools/render/` に実装を置く**。
サービスを乗り換えるとき、書き換わるのはホスト側だけである。

## 🔒 契約

アダプタは**サブコマンドを持つ1つの実行可能ファイル**である。

```
<adapter> <video_dir> preflight
<adapter> <video_dir> images   [--only SC-03 ...]
<adapter> <video_dir> audio    [--only ...] [--lang <code>]
<adapter> <video_dir> retime
<adapter> <video_dir> sfx      [--only ...]
<adapter> <video_dir> bgm
<adapter> <video_dir> srt      [--lang <code>]
<adapter> <video_dir> video    [--only ...]
<adapter> <video_dir> compose  [--lang <code>]
<adapter> <video_dir> all
```

`--lang` 付きの工程は **`i18n/<lang>.json` が在るときだけ**意味を持つ（無い動画では単言語のまま）。

### 守ること

- **出力は `<video_dir>/media/` 配下の固定名だけ。** それ以外の場所に書かない
  （`<scene_id>.png` / `<scene_id>.mp3` / `<scene_id>.sfx.mp3` / `<scene_id>.mp4` / `bgm.mp3` / `final.mp4`。
  多言語ぶんは `<lang>/<scene_id>.mp3` / `<lang>.srt` / `final.audio.<lang>.mp3` — マスター言語のファイル名は据え置き、
  言語が増えてもマスターのパスが動かないことが「在るものは作り直さない」を言語追加後も成立させる）
- **在るファイルは作り直さない。** 課金が発生するので、これは実害の回避である
- **`--only` で対象を絞れる。** 1つだけ作り直したいときに全部を回さないため
- **定義を書き換えない。** `script.md` / `scenes.json` / `timeline.json` は読み取り専用
- **生成したものを実測してから報告する。** 長さ・サイズ・寸法。API の 200 は「作れた」ではない
- **サービスの制約は、アダプタが吸収する。** 例: クリップ長が離散値しか取れないなら、
  大きい側で作って切り詰める。**その事情を型の側に漏らさない**
- **秘密を、git に追跡されるファイルから読まない**（下記）
- **`preflight` は課金の前に叩かれる。** 何も生成せず、何も課金しない

### 多言語（`--lang`）の契約

全言語が**1本の映像とマスターの実測タイムラインを共有する**（[retime.md](retime.instructions.md)）。アダプタが守るのは:

- **`audio --lang <code>`** … `i18n/<lang>.json` のナレーションを、マスターと同じ声の設定で
  `media/<lang>/<scene_id>.mp3` に生成する。生成後に**必ず実測し**、そのシーンの窓
  （`timeline.actual.json` の尺から発話開始のオフセットを引いた長さ）に**収まらないものは報告する**。
  **黙って速度を変えて詰め込まない・切り詰めない**——それは翻訳側（定義）の欠陥であり、
  実測で押し通すと不自然な早口が完成物に残る。短い側は無音で埋まるだけなので欠陥ではない。
- **`srt --lang <code>`** … `timeline.actual.json` の実測秒と、caption（マスター言語は `assets/*.json`、
  他言語は `i18n/<lang>.json`）から字幕ファイル `media/<lang>.srt` を機械的に起こす。
  **外部サービスを叩かず、課金ゼロ**（在れば作り直してよい唯一の例外ではなく、消して再実行の規律は同じ）。
- **`compose --lang <code>`** … 言語別の完成音声 `media/final.audio.<lang>.mp3` を、
  マスターと同じ間・同じ BGM/効果音の設計で合成する。**映像は言語で分けない**
  （複数音声トラックを1本の動画に載せる配信形態が前提。映像に焼く文字をどの言語にするかは
  ホスト／チャンネルの方針であり、この契約は定めない）。

### 🔒 秘密（API キー）の扱い

**アダプタは、git に追跡されるファイルから秘密を読まない。**

禁じているのは**読む場所**だけで、供給経路は縛らない。ホストが `.env` を読んで環境変数に載せてもよいし、
シェルや OS のキーチェーンから渡してもよい。「環境変数から受け取れ」ではなく
**「追跡されるファイルから読むな」**と書くのは、供給手段を将来変えても契約が生き残るためである。

**設定と秘密を同じ器に入れない。** 両者は追跡の要求が正反対である。

| | 追跡 | 中身 |
| --- | --- | --- |
| **設定** | **する** | どのサービス・モデルID・声ID・解像度。追跡しないと「なぜこの回だけ違うのか」が追えない |
| **秘密** | **しない** | API キー |

同じファイルに入れると、gitignore すれば設定の再現性が消え（穴埋めの `*.example.*` は必ず本体とズレる）、
追跡すればいつかキーが入る。**設定には秘密の値ではなく、秘密の在り処**（環境変数名など）を書く。

そしてこの型は、**エージェントがファイルを読む前提で動いている**。`media-checker` はホストを歩き回る。
**平文のキーがリポジトリ内のファイルに在れば、エージェントの視野に入る**——
grep の巻き添えで会話に出るし、成果物に書き出される経路もある。git に漏れるより先に、こちらが起きる。

**config のファイル名もキー名もスキーマも、この型は定めない。** サービス数も設定項目も知らないので、
形を決めるのは越権である。決めるのはホストである。

### 🔒 `preflight`（課金の前に止まる）

**必要なキーと設定が揃っているかを、何も生成せず・何も課金せずに確かめる。**

工程は `画像 → 音声 → …` の順に走るので、**音声のキーが無いことは音声工程で初めて分かる**。
それでは**画像を生成して課金した後に止まる**。skill は工程1で課金の規模を提示して承認を取るが、
**その承認の前提（そもそも全工程を走れるのか）を確かめないまま承認させることになる**。

- **全工程ぶんまとめて確かめる。** これから叩く工程だけでなく、`compose` までに要るものを見る
- **足りないものを、名前を挙げて報告する。** 「設定してください」では、何を設定するのか分からない
- **確かめられるのは「在ること」までである。** キーが有効か・残高が足りるかは実際に叩くまで分からない。
  **残高切れによる途中終了は、この工程では防げない**（防げるふりをしない）

## 報告すること

各工程の最後に、**何を作り、何を飛ばし、実測がいくつだったか**を出す。
飛ばしたものは「すでに在る」と明示する（黙って飛ばすと、作られたのかどうかが分からない）。

## サービスの制約は必ずある（吸収の例）

実際に当たったもの:

- **画像 API にネガティブプロンプトが無い。** 定義の `image.negative` を、
  正のプロンプトへ文章として畳み込む
- **動画のクリップ長が離散値**（4 / 6 / 8 秒しか受けない）。
  必要な長さ以上の値で作って切り詰める。**引き伸ばさない**（絵が破綻する）
- **上限を超える長さが要る**とき。切り詰めでは解決しないので、
  複数クリップを繋ぐか、静止画を継ぎ足すか、そのコマだけ静止画にするかを**人間に選ばせる**

**アダプタが黙って劣化させない。** 尺が足りない・比率が違う・ネガティブが効かないといった
「望んだものが作れなかった」場合、**それを報告する**。黙って近いもので埋めると、
出来上がってから気づくことになる。
